<%= semantic_form_for [:admin, @album] do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= cl_image_upload_tag(:image_id, :resource_type => "img") %>
  <% end %>
  <%= f.actions :submit %>
<% end %>